# 模块化单体，不拆微服务

一个后端服务，一个功能一个 Nest Module，共用一套登录、错误格式、数据库连接。

## 为什么

「后续还会集成其他功能」听起来像是拆服务的理由，实际恰恰相反：**功能越杂、
单个功能越小，拆服务的边际成本越高**。每个新工具都要额外付一份部署、一份配置、
一份鉴权打通的税，而这个站的全部负载一个进程绰绰有余。

等某个工具真的大到需要独立伸缩再拆。这个仓库大概率永远走不到那一步。

## 代价

所有功能共享一个进程的故障域。一个模块把进程搞崩，全站一起下线。
对个人工具这是可接受的 —— 反正也没有"部分可用"这种需求。
