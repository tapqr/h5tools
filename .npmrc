# 本仓库必须用 npm >= 12 安装。
#
# 系统自带的 npm 10.x 在解析 vite 8 devtools 链的可选 peer 环时会崩:
#   TypeError: Cannot read properties of null (reading 'edgesOut')
# 触发点是 @vitejs/devtools-vitest 声明 peer vitest@*，而 vitest 又把
# @vitest/browser-* 系列声明为钉死版本的可选 peer，形成跨大版本的环。
#
# 这是 npm 自身的 bug，不是依赖冲突。不要用 --legacy-peer-deps 绕过 ——
# 那会关掉全部 peer 检查，把真实冲突一起藏起来。
#
# 装依赖:  npx -y npm@latest install
engine-strict=true
